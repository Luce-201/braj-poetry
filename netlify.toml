[build]
  command = "hugo --minify"
  publish = "public"

[context.production.environment]
  HUGO_VERSION = "0.154.5"
  HUGO_ENV = "production"
```

The key change is `HUGO_VERSION = "0.154.5"` — matching exactly what's on your machine. Then push:
```